require 'spec_helper'

describe Escapement do
  it 'has a version number' do
    expect(Escapement::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
